import React from "react";
import "./Button.css";

const Button = ({ title }) => {
  return (
    <>
      <button>
        <h2>Create account</h2>
      </button>
    </>
  );
};

export default Button;